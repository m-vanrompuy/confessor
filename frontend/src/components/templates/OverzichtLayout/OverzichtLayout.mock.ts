import { ToolbarMock } from '../../organisms/Toolbar/Toolbar.mock'
import { ConfessionListMock } from '../../organisms/ConfessionList/ConfessionList.mock'
import type { OverzichtLayoutInterface } from './OverzichtLayout.interface'

export const OverzichtLayoutMock: OverzichtLayoutInterface = {
  toolbar: ToolbarMock,
  list: ConfessionListMock,
}
